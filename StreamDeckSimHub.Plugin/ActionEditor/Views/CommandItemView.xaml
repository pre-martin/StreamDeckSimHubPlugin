<UserControl x:Class="StreamDeckSimHub.Plugin.ActionEditor.Views.CommandItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.Views"
             xmlns:viewModels="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.ViewModels"
             xmlns:controls="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.Views.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:CommandItemViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="550">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModels:CommandItemKeypressViewModel}">
            <local:CommandItemKeypressView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:CommandItemSimHubControlViewModel}">
            <local:CommandItemSimHubControlView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:CommandItemSimHubRoleViewModel}">
            <local:CommandItemSimHubRoleView />
        </DataTemplate>
    </UserControl.Resources>

    <!--
        <TabControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Padding="5,10" Height="Auto">
            <TabItem Header="Element" />
            <TabItem Header="Modifiers" />
        </TabControl>
    -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Name -->
            <RowDefinition Height="Auto" /> <!-- Detail data (see DataTemplates above) -->
            <RowDefinition Height="Auto" /> <!-- Conditions -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0">Item Name:</Label>
        <TextBox Grid.Row="0" Grid.Column="1" MinWidth="150"
                 VerticalContentAlignment="Center"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                 ToolTip="Is only used for the list on the left side!" />

        <ContentControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="{Binding}">
            <d:ContentControl.Content>
                <Border Padding="10" BorderBrush="Gray" BorderThickness="1" Margin="0,5">
                    <TextBlock Text="Placeholder for concrete CommandItem" />
                </Border>
            </d:ContentControl.Content>
        </ContentControl>

        <controls:ConditionsControl Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,20,0,0" />
    </Grid>
</UserControl>