<UserControl x:Class="StreamDeckSimHub.Plugin.ActionEditor.Views.DisplayItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.Views"
             xmlns:viewModels="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:DisplayItemViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="550">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModels:DisplayItemImageViewModel}">
            <local:DisplayItemImageView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:DisplayItemTextViewModel}">
            <local:DisplayItemTextView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:DisplayItemValueViewModel}">
            <local:DisplayItemValueView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Name -->
            <RowDefinition Height="Auto" /> <!-- Detail data (see DataTemplates above) -->
            <RowDefinition Height="Auto" /> <!-- Display Parameters -->
            <RowDefinition Height="Auto" /> <!-- Conditions -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0">Name:</Label>
        <TextBox Grid.Row="0" Grid.Column="1" MinWidth="150"
                 VerticalContentAlignment="Center"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                 ToolTip="Is only used for the list on the left side!" />

        <ContentControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="{Binding}" />

        <local:DisplayParametersView Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,20,0,0" />

        <local:ConditionsView Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,20,0,0" />
    </Grid>
</UserControl>