<UserControl x:Class="StreamDeckSimHub.Plugin.ActionEditor.Views.CommandItemKeypressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.ViewModels"
             xmlns:controls="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.Views.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:CommandItemKeypressViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="550">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Margin="0,10,0,0">Keypress:</Label>
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox Width="50" Height="24"
                     Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center" />
            <CheckBox IsChecked="{Binding ModifierCtrl}" Margin="30,0,0,0"
                      VerticalAlignment="Center">
                Ctrl
            </CheckBox>
            <CheckBox IsChecked="{Binding ModifierAlt}" Margin="10,0,0,0"
                      VerticalAlignment="Center">
                Alt
            </CheckBox>
            <CheckBox IsChecked="{Binding ModifierShift}" Margin="10,0,0,0"
                      VerticalAlignment="Center">
                Shift
            </CheckBox>
        </StackPanel>

        <controls:LongPressControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,0"
                                   Visibility="{Binding LongPressAllowed, Converter={StaticResource BooleanToVisibilityConverter}}" />

    </Grid>
</UserControl>