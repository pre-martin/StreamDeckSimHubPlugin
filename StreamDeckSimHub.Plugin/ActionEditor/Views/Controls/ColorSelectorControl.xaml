<UserControl x:Class="StreamDeckSimHub.Plugin.ActionEditor.Views.Controls.ColorSelectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.Converters"
             xmlns:viewModels="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:IColorSelectable, IsDesignTimeCreatable=False}"
             d:DesignHeight="50" d:DesignWidth="750">

    <UserControl.Resources>
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Margin="0,10,0,0">Color:</Label>
        <StackPanel Grid.Column="1" Margin="0,10,0,0" HorizontalAlignment="Left" Orientation="Horizontal">
            <Rectangle Width="80" Height="24"
                      Stroke="Black" StrokeThickness="1"
                      Fill="{Binding ColorAsWpf, Converter={StaticResource ColorToBrushConverter}}"
                      Cursor="Hand" MouseDown="ColorRectangle_MouseDown" />
            <TextBox Text="{Binding ColorHex, Mode=OneWay}" Width="80" Margin="50,0,0,0"
                    IsReadOnly="True"
                    VerticalContentAlignment="Center" HorizontalContentAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
