<UserControl x:Class="StreamDeckSimHub.Plugin.ActionEditor.Views.Controls.FontSelectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:IFontSelectable, IsDesignTimeCreatable=False}"
             d:DesignHeight="50" d:DesignWidth="550">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Margin="0,10,0,0">Font:</Label>
        <Label Grid.Column="1" Margin="0,10,0,0" Width="210"
               HorizontalAlignment="Left" VerticalContentAlignment="Center"
               Content="{Binding FontAsString}"
               BorderThickness="1"
               BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
               Cursor="Hand" MouseDown="FontLabel_MouseDown" />
    </Grid>
</UserControl>
