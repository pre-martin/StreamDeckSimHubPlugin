<UserControl x:Class="StreamDeckSimHub.Plugin.ActionEditor.Views.ModifierBlinkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.ViewModels"
             xmlns:controls="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.Views.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:ModifierBlinkViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="750">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Detail data -->
            <RowDefinition Height="Auto" /> <!-- Conditions -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Margin="0,10,0,0">Duration On:</Label>
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBox Width="50" Text="{Binding DurationOn, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"
                         Margin="0,0,0,0" />
                <TextBlock VerticalAlignment="Center" Margin="10,0,0,0">x 100 ms</TextBlock>
            </StackPanel>

            <Label Grid.Row="1" Grid.Column="0" Margin="0,10,0,0">Duration Off:</Label>
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBox Width="50" Text="{Binding DurationOff, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"
                         Margin="0,0,0,0" />
                <TextBlock VerticalAlignment="Center" Margin="10,0,0,0">x 100 ms</TextBlock>
            </StackPanel>
        </Grid>

        <controls:ExpressionControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,20,0,0"
                                 DataContext="{Binding ExpressionControlConditionViewModel}" />

    </Grid>
</UserControl>
