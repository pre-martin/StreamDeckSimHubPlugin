<UserControl x:Class="StreamDeckSimHub.Plugin.ActionEditor.Views.DisplayItemBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.ViewModels"
             xmlns:controls="clr-namespace:StreamDeckSimHub.Plugin.ActionEditor.Views.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:DisplayItemBoxViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="750">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <controls:ColorSelectorControl Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" />

        <Label Grid.Row="1" Grid.Column="0" Margin="0,10,0,0">Corner Radius:</Label>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox Width="50" Text="{Binding CornerRadiusText, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                     VerticalContentAlignment="Center" />
            <Label FontWeight="Light" Margin="80,0,0,0" Content="{Binding CornerRadiusToolTip}" />
        </StackPanel>

    </Grid>

</UserControl>