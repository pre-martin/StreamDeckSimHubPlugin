<Window x:Class="StreamDeckSimHub.Plugin.ActionEditor.ImageSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Activated="Window_Activated" Loaded="Window_Loaded"
        Icon="/ActionEditor/GenericButtonEditor.ico"
        Title="Select Image" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" /> <!-- Labels -->
            <ColumnDefinition Width="Auto" /> <!-- Fields -->
            <ColumnDefinition Width="*" />    <!-- Remainder -->
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Margin="0,0,20,0">Directory:</Label>
        <ComboBox Grid.Row="0" Grid.Column="1" Name="DirectoryComboBox" MinWidth="180" SelectionChanged="DirectoryComboBox_OnSelectionChanged" />

        <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,20">
            <WrapPanel Orientation="Horizontal" Name="ImagesPanel" />
        </ScrollViewer>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="OkButton" MinWidth="50" Margin="16,0,0,0" IsDefault="True" Click="OkButton_OnClick">OK</Button>
            <Button Name="Cancel" MinWidth="50" Margin="16,0,0,0" IsCancel="True">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
